# 犬の体重管理ツール

## 表示確認URL
ローカル環境で次を実行すると、ブラウザで表示を確認できます。

```bash
python3 -m http.server 8000
```

表示URL: http://localhost:8000/

## 補足
- スマホ確認: 同一ネットワーク上でPCのIPを使って `http://<PCのIPアドレス>:8000/` にアクセス
- 停止: `Ctrl + C`
